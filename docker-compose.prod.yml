version: "3.8"

volumes:
  backend_storage:

services:
  tds_backend:
    container_name: tds_backend_prod
    build:
      context: ./backend
      dockerfile: Dockerfile
    env_file: .env
    restart: always

    environment:
      DB_HOST: ${DB_HOST}
      DB_PORT: ${DB_PORT:-3306}
      DB_USER: ${DB_USER}
      DB_PASS: ${DB_PASSWORD}
      DB_NAME: ${DB_NAME:-tds}

      SSO_BASE_URL: ${SSO_BASE_URL:-http://host.docker.internal:5000}
      SSO_FRONTEND_URL: ${SSO_FRONTEND_URL:-http://localhost:5173}
      SSO_CLIENT_ID: ${SSO_CLIENT_ID:-client_29f1ebacc189d8f2162d95ea63acd6a5}
      SSO_CLIENT_SECRET: ${SSO_CLIENT_SECRET:-b919281f18a32cc008ba63016f7d89042bb006f1ae5f0748ab83049a1d8698d4}
      SSO_CALLBACK_URL: ${SSO_CALLBACK_URL:-https://pe.spil.co.id/tds-api/api/auth/sso/callback}
      FRONTEND_URL: ${FRONTEND_URL:-https://pe.spil.co.id/tds}

      WEB_PORT: 8080
      ENV: production
      GIN_MODE: release

      JWT_SECRET_KEY: ${JWT_SECRET}

      GEMINI_API_KEY: ${GEMINI_API_KEY}
      GEMINI_MODEL: ${GEMINI_MODEL}

      BACKEND_PUBLIC_URL: ${BACKEND_PUBLIC_URL:-https://pe.spil.co.id/tds-api}

    volumes:
      - backend_storage:/app/storage

    expose:
      - "8080"

    networks:
      - traefik-net

    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 40s

    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=traefik-net"
      - "traefik.http.routers.tds-backend.rule=Host(`pe.spil.co.id`) && PathPrefix(`/tds-api`)"
      - "traefik.http.routers.tds-backend.entrypoints=websecure"
      - "traefik.http.routers.tds-backend.tls=true"
      - "traefik.http.routers.tds-backend.middlewares=strip-tds-api"
      - "traefik.http.middlewares.strip-tds-api.stripprefix.prefixes=/tds-api"
      - "traefik.http.services.tds-backend.loadbalancer.server.port=8080"

  tds_frontend:
    container_name: tds_frontend_prod
    build:
      context: ./frontend
      dockerfile: Dockerfile.prod
      args:
        NEXT_PUBLIC_API_ENDPOINT: ${NEXT_PUBLIC_API_ENDPOINT:-https://pe.spil.co.id/tds-api}
        NEXT_PUBLIC_BASE_PATH: ${NEXT_PUBLIC_BASE_PATH:-}

    env_file: .env
    restart: always

    environment:
      NODE_ENV: production
      NEXT_PUBLIC_API_ENDPOINT: ${NEXT_PUBLIC_API_ENDPOINT:-https://pe.spil.co.id/tds-api}
      NEXT_PUBLIC_BASE_PATH: ${NEXT_PUBLIC_BASE_PATH:-}
      JWT_SECRET: ${JWT_SECRET}

    depends_on:
      tds_backend:
        condition: service_healthy

    networks:
      - traefik-net

    expose:
      - "3000"

    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=traefik-net"
      - "traefik.http.routers.tds-frontend.rule=Host(`pe.spil.co.id`) && PathPrefix(`/tds`)"
      - "traefik.http.routers.tds-frontend.entrypoints=websecure"
      - "traefik.http.routers.tds-frontend.tls=true"
      - "traefik.http.routers.tds-frontend.middlewares=strip-tds"
      - "traefik.http.middlewares.strip-tds.stripprefix.prefixes=/tds"
      - "traefik.http.services.tds-frontend.loadbalancer.server.port=3000"

networks:
  traefik-net:
    name: traefik-net
    external: true
